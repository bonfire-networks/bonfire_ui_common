<aside class={@class} aria-label={@widget_title || nil}>
  <div :if={@widget_title} class={"flex items-center justify-between", @title_class}>
    <div>{@widget_title}</div>
    <#slot {@action} />
  </div>
  <#slot />
</aside>
