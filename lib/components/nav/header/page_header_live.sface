<!-- <div class="relative"> -->
<div role="banner" class="">
  <div class="flex items-center">
    <div class="flex items-center flex-1 space-x-3 text-left">
      <#slot {@left_action}>
      </#slot>

      <div class="flex-1">
        <div class="text-sm breadcrumbs">
          <ul>
            {#case Bonfire.Common.ExtensionModules.extension(@__context__[:current_app])}
              {#match %{name: name} = extension}
                <li>
                  <LiveRedirect to={extension[:href] || "/"}>
                    <Icon :if={extension[:icon]} iconify={extension[:icon]} class="w-5 h-5 pr-2" />
                    {name}
                  </LiveRedirect>
                </li>
                <li class="truncate text-ellipsis" :if={not is_nil(@page_title) and @page_title != name}>{@page_title}</li>
              {#match _}
                <li class="truncate text-ellipsis" :if={@page_title}>{@page_title}</li>
            {/case}
          </ul>
        </div>
      </div>

      <#slot {@right_action}>
      </#slot>
    </div>
  </div>
  <Bonfire.UI.Common.PreviewContentLive id="preview_content" />
</div>
<!-- </div> -->